# Build output
bin/
obj/
[Bb]in/
[Oo]bj/
publish/
**/win-x64/

# Visual Studio / user-specific
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Regenerable STIG enrichment cache (rebuilt by tools/Generate-StigCatalog.ps1)
tools/.cache/

# Build/publish artifacts stay out of git — except the shipped release zip,
# which is committed at the repo root (also attached to the GitHub Release).
*.zip
!/AtlantHarden.zip

# Scratch / temp
_stigdata/
_settings.json

# OS
Thumbs.db
Desktop.ini
.DS_Store
