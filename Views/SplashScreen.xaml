<Window x:Class="AtlantHarden.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Width="450" Height="320"
        ResizeMode="NoResize"
        Topmost="True">
    
    <Border Background="#0D1117" CornerRadius="16" BorderBrush="#30363D" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="#000000"/>
        </Border.Effect>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Logo and Title -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
                <Image Source="/Resources/logo.png" Height="48" Margin="0,0,0,12"/>
                <TextBlock Text="AtlantHarden" FontSize="22" FontWeight="Bold" 
                           Foreground="#F0F6FC" HorizontalAlignment="Center"/>
                <TextBlock Text="Windows 11 Security Hardening" FontSize="12" 
                           Foreground="#8B949E" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>
            
            <!-- Animated Shield Icon -->
            <Border Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" 
                    Width="70" Height="70" CornerRadius="35" Background="#161B22">
                <TextBlock x:Name="ShieldIcon" Text="🛡️" FontSize="32" 
                           HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock.RenderTransform>
                        <ScaleTransform x:Name="PulseTransform" ScaleX="1" ScaleY="1" CenterX="16" CenterY="16"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Border>
            
            <!-- Phase Indicator -->
            <Border Grid.Row="3" Background="#161B22" CornerRadius="6" Padding="12,6" 
                    HorizontalAlignment="Center" Margin="0,16,0,8">
                <TextBlock x:Name="PhaseText" Text="Initializing..." 
                           FontSize="11" Foreground="#58A6FF" HorizontalAlignment="Center"/>
            </Border>
            
            <!-- Status Message -->
            <TextBlock Grid.Row="4" x:Name="StatusText" Text="Loading security settings..." 
                       FontSize="13" Foreground="#8B949E" HorizontalAlignment="Center" 
                       TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,12"/>
            
            <!-- Progress Bar -->
            <Grid Grid.Row="5" Margin="0,0,0,8">
                <Border Background="#21262D" CornerRadius="4" Height="8"/>
                <Border x:Name="ProgressFill" CornerRadius="4" Height="8" 
                        HorizontalAlignment="Left" Width="0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#238636" Offset="0"/>
                            <GradientStop Color="#2EA043" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
            </Grid>
            
            <!-- Progress Text -->
            <TextBlock Grid.Row="6" x:Name="ProgressText" Text="0%" 
                       FontSize="11" Foreground="#6E7681" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation 
                        Storyboard.TargetName="PulseTransform"
                        Storyboard.TargetProperty="ScaleX"
                        From="1" To="1.15" Duration="0:0:0.5" AutoReverse="True"/>
                    <DoubleAnimation 
                        Storyboard.TargetName="PulseTransform"
                        Storyboard.TargetProperty="ScaleY"
                        From="1" To="1.15" Duration="0:0:0.5" AutoReverse="True"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
